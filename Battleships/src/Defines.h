#pragma once

const int SCREEN_HEIGHT = 1250;
const int SCREEN_WIDTH = 850;
const float FPS = 60.0f;

const int CELL_SIZE = 50;
const int FIELDS = 10;
const int NUM_OF_SHIPS = 10;
const int GRID_SIZE = CELL_SIZE * (FIELDS + 1);